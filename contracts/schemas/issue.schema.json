{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.local/issue/v1",
  "title": "DailyIssue",
  "description": "Canonical contract for one daily AI-news issue. Single source of truth; every downstream artifact (publishablePost.md/html, voiceText, GeneratedContent cards, RSS/JSON-Feed, video plan) is a deterministic projection of this document. Text fields use ONE inline dialect: CommonMark inline subset — **bold**, `code`, [text](url). No block markup inside strings. Spoken text (voice) is plain text, numbers already written in speakable form.",
  "type": "object",
  "required": ["schema", "date", "lang", "sections", "items"],
  "additionalProperties": false,
  "properties": {
    "schema": { "const": "issue/1" },
    "date": { "type": "string", "format": "date" },
    "weekday": { "type": "string" },
    "lang": { "type": "string", "default": "zh-CN" },
    "issue_url": { "type": "string", "format": "uri" },
    "video_links": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "bilibili": { "type": "string", "format": "uri" },
        "youtube": { "type": "string", "format": "uri" }
      }
    },
    "cover": { "$ref": "#/$defs/media" },
    "degraded": { "type": "boolean", "default": false,
      "description": "digest 降级标记：LLM 调用失败/部分字段兜底时置 true，下游 UI 可显式提示" },
    "sections": {
      "type": "array",
      "minItems": 1,
      "description": "Ordered taxonomy for THIS issue. Order in array = render order. Item.section must equal one of these slugs (checked by validator, cross-field).",
      "items": { "$ref": "#/$defs/section" }
    },
    "intro": { "$ref": "#/$defs/segment" },
    "outro": { "$ref": "#/$defs/segment" },
    "items": {
      "type": "array",
      "minItems": 1,
      "description": "Order in array = canonical order; the #N numbering in renders is derived from position, never stored.",
      "items": { "$ref": "#/$defs/item" }
    }
  },
  "$defs": {
    "section": {
      "type": "object",
      "required": ["slug", "name"],
      "additionalProperties": false,
      "properties": {
        "slug": { "type": "string", "pattern": "^[a-z0-9-]{2,24}$" },
        "name": { "type": "string" },
        "icon": { "type": "string" }
      }
    },
    "source": {
      "type": "object",
      "required": ["url"],
      "additionalProperties": false,
      "properties": {
        "url": { "type": "string", "format": "uri" },
        "label": { "type": "string" },
        "kind": {
          "enum": ["official", "repo", "paper", "media", "social", "community", "other"],
          "description": "drives link-check + screenshot policy: e.g. social/x.com unreachable from CN network -> skip shot or use mirror"
        },
        "primary": { "type": "boolean", "description": "exactly one primary source per item; drives the 相关链接 lead link and the default screenshot target" },
        "reachable": { "type": "boolean", "description": "set by the fetch/link-check stage; renderers may warn or drop unreachable sources" }
      }
    },
    "media": {
      "type": "object",
      "required": ["kind", "src"],
      "additionalProperties": false,
      "properties": {
        "kind": { "enum": ["image", "gif", "shot", "cover"] },
        "src": { "type": "string", "description": "local/mirrored path or absolute URL" },
        "origin": { "type": "string", "format": "uri", "description": "where the asset came from (source page)" },
        "alt": { "type": "string" }
      }
    },
    "card": {
      "type": "object",
      "required": ["label", "body"],
      "additionalProperties": false,
      "properties": {
        "label": { "type": "string", "description": "2-7 chars; maps to GeneratedContent.cards[].title" },
        "body": { "type": "string", "description": "30-60 chars, inline-markdown subset; maps to GeneratedContent.cards[].desc after md->html-inline conversion" },
        "icon": { "type": "string", "description": "Material Symbols name, snake_case; falls back to 'article' in renderer" }
      }
    },
    "segment": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "voice": { "type": "array", "items": { "type": "string" } },
        "note": { "type": "string" }
      }
    },
    "item": {
      "type": "object",
      "required": ["id", "section", "headline", "tldr", "body", "sources", "confidence"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9-]{2,24}$",
          "description": "stable slug; EVERY downstream artifact keys on this (cards, voice files, frames, timeline, dedup history)"
        },
        "section": { "type": "string", "description": "must equal a slug declared in issue.sections" },
        "nav": { "type": "string", "description": "short label for video nav bar / breadcrumb / overview list" },
        "headline": { "type": "string" },
        "title_short": { "type": "string", "description": "optional shorter card title for GeneratedContent.mainTitle; renderer falls back to headline" },
        "tldr": { "type": "string", "description": "1-2 dense sentences; renders as the blockquote lead" },
        "body": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string" },
          "description": "paragraphs, inline-markdown subset; the written-text spine of the item"
        },
        "sources": {
          "type": "array",
          "items": { "$ref": "#/$defs/source" },
          "description": "SHOULD have >=1; empty is legal (e.g. deleted source post — real case: 2026-09-20 #14 kimi) but the validator warns"
        },
        "media": { "type": "array", "items": { "$ref": "#/$defs/media" } },
        "confidence": {
          "enum": ["confirmed", "reported", "rumor", "speculation"],
          "description": "official fact vs media report vs leak/speculation; enforces the hedging rule in renders and lets video style differ"
        },
        "entities": {
          "type": "array",
          "items": { "type": "string" },
          "description": "optional proper-noun list (models, companies, people); used by dedup/clustering and the no-new-entities validator"
        },
        "facts": {
          "type": "array",
          "items": { "type": "string" },
          "description": "optional claim whitelist: precise fragments the editor stands behind (e.g. '42.5万例增强腹部CT', '总参数29B 激活4B'). voice/cards may use numbers from headline+tldr+body+facts; anything else is flagged. This is the deterministic anti-hallucination hook for downstream LLM stages."
        },
        "voice": {
          "type": "array",
          "items": { "type": "string" },
          "description": "PROJECTION, filled by the voice-script stage; one entry = one TTS sentence; plain text only"
        },
        "cards": {
          "type": "array",
          "maxItems": 8,
          "items": { "$ref": "#/$defs/card" },
          "description": "PROJECTION, filled by the card stage; contract-compatible with juya-news-card GeneratedContent"
        },
        "video": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "shot_sentences": {
              "type": "array",
              "items": { "type": "integer", "minimum": 1 },
              "description": "1-based indexes into voice[] during which the source-page screenshot overlay shows; production hint filled at video-plan stage"
            }
          }
        }
      }
    }
  }
}
